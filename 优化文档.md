# Project Optimization Report

## 一、项目当前总体判断

当前项目已经完成了首页、任务、错题、知识点、搜索、统计、数据管理等核心页面雏形，也已经开始接入 Supabase。  
但从代码结构、页面一致性和长期维护角度看，项目已经出现明显的“继续堆功能会越来越乱”的趋势。

当前最核心的问题不是“功能太少”，而是：

- 数据源没有真正统一
- 页面结构没有真正统一
- 组件规范没有真正统一
- 视觉系统没有真正收口
- page client 容器承担了过多职责

如果不先解决这些问题，后面每加一个功能页，都只是在复制现有问题。

### 1.1 总览评级

| 维度 | 当前状态 | 评级 | 说明 |
|---|---|---|---|
| 产品方向 | 明确 | 好 | 个人考研学习工具定位清晰，没有跑偏 |
| MVP 范围 | 基本完整 | 好 | 任务、记录、搜索、统计、导入导出均已覆盖 |
| 页面完成度 | 中等 | 中 | 页面可访问，但结构和一致性不足 |
| 视觉统一性 | 偏弱 | 差 | 首页、模块页、统计页风格割裂 |
| 数据层稳定性 | 偏弱 | 差 | mock、本地缓存、Supabase 三套并存 |
| 工程可维护性 | 开始恶化 | 差 | 重复逻辑明显，容器文件过重 |
| 正式可上线性 | 不足 | 差 | 鉴权、错误处理、附件链路还不稳 |

### 1.2 优先级总表

| 类别 | 严重程度 | 说明 | 优先级 |
|---|---|---|---|
| Supabase 鉴权与数据边界 | 高 | 当前接入方式不适合正式使用 | P0 |
| 数据源混乱 | 高 | mock、本地、远端同时生效 | P0 |
| 文案乱码与编码问题 | 高 | 直接影响可用性 | P0 |
| 设计 token 漂移 | 高 | 视觉系统已经失控 | P0 |
| 错误处理缺失 | 高 | 大量静默失败 | P0 |
| 页面结构不统一 | 中 | 首页与各模块页信息架构不稳定 | P1 |
| page-client 过重 | 中 | 数据、状态、布局全部堆在一起 | P1 |
| 组件复用不足 | 中 | 相同模式被重复实现 | P1 |
| 移动端体验不佳 | 中 | 详情和筛选不符合 Mobile First | P1 |
| 功能闭环不完整 | 中 | 上传、归档、设置等链路不完整 | P1 |
| 无障碍与细节体验 | 低 | 可后补，但不能一直忽略 | P2 |
| 性能优化 | 低 | 当前不是主矛盾 | P2 |

---

## 二、P0 阻塞问题

### 2.1 Supabase 接入方式需要立即收紧

| 问题 | 当前情况 | 风险 | 优化建议 |
|---|---|---|---|
| API 鉴权缺失 | 路由层主要依赖环境变量存在与否 | 正式部署后存在越权风险 | 接入 `Supabase Auth + user_id + RLS` |
| service role 直接承担业务访问 | API 直接透传高权限访问数据库 | 业务边界过于宽松 | 改为最小权限服务端访问 |
| 缺少服务端输入校验 | POST/PATCH 数据几乎裸写 | 易写入脏数据 | 给任务、学习记录、上传接口加 schema 校验 |

### 2.2 数据源混乱必须先统一

| 问题 | 当前情况 | 风险 | 优化建议 |
|---|---|---|---|
| mock、本地缓存、Supabase 共存 | 每页都可能读到不同来源 | 搜索、统计、导出结果失真 | 明确单一事实来源 |
| Supabase 空库不接管页面 | 远端为空时页面继续显示 mock | fresh project 仍展示假数据 | 远端为空也应视为真实状态 |
| 页面各自处理同步 | 每个页面都自己拉取和写回 | 重复逻辑越来越多 | 抽共享数据同步 hook |

### 2.3 文案乱码必须优先清理

| 问题范围 | 当前情况 | 影响 | 优化建议 |
|---|---|---|---|
| 导航 | 侧边栏、顶栏存在乱码 | 影响基础使用 | 统一修正编码并逐一替换 |
| 页面标题 | 首页、任务、搜索、统计等文案有乱码 | 专业感和可读性下降 | 统一文案层 |
| 按钮与提示 | 删除、归档、说明文案存在乱码 | 影响关键操作 | 优先修高频交互文案 |

### 2.4 样式 token 已经漂移

| 问题 | 当前情况 | 影响 | 优化建议 |
|---|---|---|---|
| 未定义类名被频繁使用 | `bg-moss-*`、`shadow-float`、`safe-bottom` 等 | 实际渲染不可控 | 统一回收并重新定义 |
| 全局设计 token 太少 | 只有 `.panel`、`.soft-pill` 等少量样式 | 无法支撑完整 UI 统一 | 建立颜色、阴影、圆角、间距体系 |
| 模块样式靠局部硬写 | 各卡片各自组合 Tailwind 类 | 后续统一设计成本极高 | 提升通用 UI 组件层 |

### 2.5 错误处理不能继续静默

| 问题 | 当前情况 | 影响 | 优化建议 |
|---|---|---|---|
| `.catch(() => undefined)` 很多 | 失败后用户无感知 | 数据可能没保存但 UI 看似成功 | 增加统一错误提示 |
| 乐观更新无回滚 | 本地先变、远端失败不恢复 | 本地与远端状态分叉 | 增加回滚或失败提示 |
| 无 loading / error / retry | 只有成功路径 | 交互体验不完整 | 建立请求状态规范 |

---

## 三、页面结构与信息架构问题

### 3.1 首页问题

| 问题 | 当前情况 | 建议 |
|---|---|---|
| 首页没有完全按原产品结构落地 | `DatePanel`、`QuickAccessCard` 存在但未真正接入首页主流程 | 重新组织为 `日期 / 倒计时 / 今日任务 / 随机知识点 / 快速入口` 五区结构 |
| 首页职责漂移 | 路由文件里内联了 StudyProgress 区块 | 把统计摘要和首页概览分离 |
| 首页与统计页口径脱节 | 首页有静态进度和热力展示，统计页又有正式统计模块 | 首页仅保留摘要，完整趋势交给统计页 |

### 3.2 任务页问题

| 问题 | 当前情况 | 建议 |
|---|---|---|
| 手机端不是列表优先 | 先概览再筛选再列表 | 手机端应改成任务列表优先 |
| 详情区不适合手机 | 手机端详情直接堆在列表后面 | 用全屏详情或抽屉详情代替 |
| 任务步骤、状态、进度展示分散 | 相关 UI 逻辑散落在列表卡和详情卡 | 抽统一任务进度组件 |

### 3.3 错题页与知识点页问题

| 问题 | 当前情况 | 建议 |
|---|---|---|
| 两页结构过于平行 | 只是字段不同，却各自实现一整套页面 | 抽共享的 `List + Detail` 壳层 |
| 筛选面板重复实现 | 来源、标签、归档、类型等模式重复 | 抽通用筛选字段组件 |
| 附件区重复实现 | 两种 attachment grid 基本同构 | 抽统一附件组件 |

### 3.4 搜索页问题

| 问题 | 当前情况 | 建议 |
|---|---|---|
| 手机端筛选过重 | 所有筛选项默认展开 | 改为“搜索优先，筛选抽屉” |
| 结果与详情联动不够稳定 | 依赖 fallback 自动选中第一条 | 明确选中态管理规则 |
| 搜索口径虽统一，查询层偏重 | 全量数据回传后再过滤 | 中期逐步下沉到 Supabase 查询 |

### 3.5 统计页问题

| 问题 | 当前情况 | 建议 |
|---|---|---|
| 首页和统计页职责重叠 | 都在展示学习进度和活跃趋势 | 首页只保留摘要，统计页负责正式复盘 |
| 图表卡风格和说明层级不够统一 | 热力图、折线图、说明卡未形成统一规范 | 建立图表卡标准样式 |

### 3.6 数据管理页问题

| 问题 | 当前情况 | 建议 |
|---|---|---|
| 单页职责偏重 | 导出、导入、预览、概览都堆在一个容器里 | 强化 tab 模式，明确“导出模式”和“导入模式” |
| 移动端信息密度偏高 | 概览与操作并列过多 | 手机端简化首屏，突出主流程 |
| 与搜索模块耦合过深 | 导出逻辑依赖搜索侧工具函数 | 重整导出逻辑边界 |

---

## 四、三端响应式问题

### 4.1 总体问题

| 终端 | 当前问题 | 应优化为 |
|---|---|---|
| 手机端 | 详情和筛选都偏重，主列表不够突出 | 单列、列表优先、全屏详情、完整底部导航 |
| 平板端 | 双栏雏形有了，但不统一 | 固定左列表右详情，筛选可折叠 |
| 电脑端 | 三段式开始出现，但规则不统一 | `概览/筛选 + 列表 + 详情` 统一布局 |

### 4.2 手机端重点问题

| 问题 | 说明 |
|---|---|
| 底部导航不完整 | `/data` 当前没有稳定手机端入口 |
| 搜索和筛选优先级错位 | 应先搜索，再展开筛选 |
| 详情展示方式不适合手机 | 更适合抽屉或全屏详情页 |
| 首页首屏内容排序不够清晰 | 应突出今日任务与高频入口 |

### 4.3 平板端重点问题

| 问题 | 说明 |
|---|---|
| 左右区比例缺少统一规则 | 任务、错题、知识点、搜索页都不完全一致 |
| 详情阅读体验未被强化 | 现在更像桌面压缩版，不像平板整理模式 |

### 4.4 电脑端重点问题

| 问题 | 说明 |
|---|---|
| 顶栏 + 页头 + 侧栏层级偏重 | 信息入口太多 |
| 仪表盘气质不统一 | 首页、统计页、数据页像不同产品 |
| 看起来像管理后台，但交互并未支撑批量管理 | 易产生认知落差 |

---

## 五、代码结构与复用问题

### 5.1 page-client 文件过重

| 文件类型 | 当前承担内容 | 问题 |
|---|---|---|
| `*-page-client.tsx` | 数据加载、筛选状态、选中状态、远端同步、布局拼装、操作行为 | 单文件承担过多职责，后期维护成本高 |

### 5.2 当前明显重复的逻辑

| 重复项 | 分布位置 | 建议 |
|---|---|---|
| `usePersistentCollection + remote fetch + mounted flag` | 任务、错题、知识点、搜索、统计、数据管理 | 抽共享同步 hook |
| `reviveTasks` | 多处重复定义 | 抽到任务模块工具层 |
| 列表 + 详情布局模式 | 任务、错题、知识点、搜索 | 抽共享布局骨架 |
| 详情区内容块容器 | 多模块重复 | 抽 `DetailSection` |
| 筛选字段 UI | 多个 filter panel 重复 | 抽 `FilterField` 级别组件 |

### 5.3 全局组件层不成熟

| 现状 | 问题 | 建议 |
|---|---|---|
| 现有全局组件主要只有 `SectionCard`、`ProgressBar`、`ProgressRing` | 能承担设计系统的基础组件过少 | 增加 `Button`、`Badge`、`Modal`、`EmptyState`、`FilterField` 等 |
| 有的组件定义了但未使用 | 如 `PageLinkCard`、`HomeHeader` | 清理废弃组件，减少噪音 |

---

## 六、功能完整性问题

### 6.1 数据仍带明显静态痕迹

| 模块 | 当前情况 | 问题 |
|---|---|---|
| 首页 | 仍有不少独立 mock 逻辑 | 与真实任务/知识点沉淀不完全同步 |
| 统计页 | 统计虽能跑，但依赖本地聚合逻辑 | 数据源统一前结果不绝对可信 |
| 倒计时/连续天数/进度摘要 | 部分仍是静态或半静态模式 | 长期使用会失真 |

### 6.2 功能入口和功能闭环不一致

| 功能 | 当前状态 | 问题 |
|---|---|---|
| 设置 | 有视觉入口，但没有正式页面闭环 | 导航不一致 |
| 通知 / 书签 | 顶栏有按钮，但没有实际功能 | 假交互 |
| 上传 | API 已存在，但前端闭环不完整 | 真正使用时会断裂 |
| 归档 | 字段有了，但没有正式归档管理体验 | 难以长期整理 |

---

## 七、视觉系统问题

### 7.1 当前视觉问题总表

| 问题 | 当前表现 | 建议 |
|---|---|---|
| 视觉语言割裂 | 首页偏氛围、模块页偏工具后台、统计页偏数据面板 | 收口成“私人学习桌面”统一气质 |
| 色彩体系漂移 | `sage/emerald/sky/violet/moss` 混用 | 固定主色、辅助色、状态色 |
| 卡片层级不一致 | 不同页面自己拼圆角、边框、渐变、阴影 | 抽卡片层级体系 |
| 字体层级不稳定 | 标题、说明、注释、标签层级无系统 | 建立类型层级规范 |

### 7.2 建议建立的设计 token

| token 类别 | 建议内容 |
|---|---|
| 颜色 token | 主背景、卡片背景、主色、辅助色、成功、警告、危险、边框、正文、次级文本 |
| 圆角 token | 页面卡片、输入框、按钮、tag、图表容器 |
| 阴影 token | 页面浮层、卡片浮层、交互悬浮态 |
| 间距 token | 页面 padding、模块 gap、卡片内边距、列表项间距 |
| 字体 token | 页面标题、模块标题、正文、注释、徽标文本 |

---

## 八、接下来如何继续优化

### 8.1 Phase 1：止血

| 任务 | 目的 | 具体动作 | 优先级 |
|---|---|---|---|
| 修复全部乱码文案 | 恢复基础可用性 | 全量改 UTF-8，逐页替换高频文案 | P0 |
| 收口设计 token | 让样式真正可控 | 补齐或替换未定义类名 | P0 |
| 统一数据源规则 | 结束三套数据并存 | 明确远端为空也覆盖本地 | P0 |
| 建立错误反馈 | 避免静默失败 | 增加 toast、错误提示、重试 | P0 |
| 补最小表单校验 | 防止脏数据进入系统 | 给任务、导入、记录表单做最小校验 | P0 |

### 8.2 Phase 2：收敛页面结构

| 任务 | 目的 | 具体动作 | 优先级 |
|---|---|---|---|
| 重构首页信息架构 | 回到产品定位 | 接入日期、快速入口、真实任务摘要 | P1 |
| 统一列表页骨架 | 减少结构分裂 | 任务、错题、知识点、搜索共用列表-详情模式 | P1 |
| 修正移动端详情策略 | 强化 Mobile First | 改为全屏详情或抽屉 | P1 |
| 收口导航层级 | 降低认知负担 | 清理重复头部方案，补齐底部导航 | P1 |

### 8.3 Phase 3：重构数据层与组件层

| 任务 | 目的 | 具体动作 | 优先级 |
|---|---|---|---|
| 抽共享数据 hook | 去掉重复同步逻辑 | 提炼 `useTasksData`、`useStudyRecordsData` 等 | P1 |
| 拆 page-client | 降低单文件复杂度 | 分为 controller hook + presentational component | P1 |
| 收敛类型层 | 提高可维护性 | 建立共享基础类型出口 | P1 |
| 收敛服务边界 | 明确职责 | 页面只调 service，service 只调 API | P1 |

### 8.4 Phase 4：补完真实使用闭环

| 任务 | 目的 | 具体动作 | 优先级 |
|---|---|---|---|
| 打通附件上传闭环 | 让图文记录真正可用 | 上传、展示、更新、删除全流程接通 | P1 |
| 完成归档管理闭环 | 提高长期整理能力 | 加归档视图和恢复流程 | P1 |
| 统一统计口径 | 首页与统计页一致 | 全部统计都从任务完成和记录数聚合 | P1 |
| 重整导入导出边界 | 提高稳定性 | 逐步把导入导出迁移到服务端主导 | P2 |

### 8.5 Phase 5：品质增强

| 任务 | 目的 | 具体动作 | 优先级 |
|---|---|---|---|
| 自定义确认弹窗 | 统一交互体验 | 替换 `window.confirm` | P2 |
| 无障碍补齐 | 提升可用性 | 补 `aria-label`、focus、键盘交互 | P2 |
| 完整状态四态 | 提升页面感知质量 | 统一 loading / empty / error / success | P2 |
| 测试与回归保护 | 降低重构风险 | 先补工具函数和关键交互测试 | P3 |

---

## 九、建议新增的共享层

| 建议位置 | 作用 |
|---|---|
| `src/hooks/use-collection-sync.ts` | 统一本地集合与远端同步逻辑 |
| `src/hooks/use-selection.ts` | 统一列表-详情页选中态逻辑 |
| `src/components/ui/button.tsx` | 统一按钮层级和状态 |
| `src/components/ui/badge.tsx` | 统一状态 badge / 标签样式 |
| `src/components/ui/modal.tsx` | 统一确认弹窗与表单弹窗 |
| `src/components/ui/empty-state.tsx` | 统一空状态 |
| `src/components/ui/filter-field.tsx` | 统一筛选控件 |
| `src/components/shared/list-detail-shell.tsx` | 统一列表-详情布局骨架 |
| `src/components/shared/detail-section.tsx` | 统一详情区内容块容器 |

---

## 十、当前不建议优先做的事情

| 项目 | 原因 |
|---|---|
| 先上 Redux / Zustand | 当前主问题不是状态库缺失，而是状态源和职责混乱 |
| 先做虚拟列表 | 当前数据量下不是最主要瓶颈 |
| 先换重型图表库 | 先统一数据口径与卡片样式更重要 |
| 先做 AI 导入 / OCR | 会显著增加复杂度，偏离当前 MVP |
| 继续新增更多页面 | 当前结构还没收紧，继续加页面只会复制问题 |

---

## 十一、推荐执行顺序

| 顺序 | 任务 |
|---|---|
| 1 | 修复乱码文案 |
| 2 | 修复 Tailwind token 漂移与未定义 class |
| 3 | 统一数据源策略 |
| 4 | 建立错误反馈与最小表单校验 |
| 5 | 重构首页信息架构 |
| 6 | 统一任务、错题、知识点、搜索四页的列表-详情骨架 |
| 7 | 抽共享 hook 和共享 UI 组件 |
| 8 | 打通上传和归档闭环 |
| 9 | 最后补无障碍、测试、PWA 细节 |

---

## 十二、一句话结论

当前项目最需要的不是“继续长功能”，而是：

**先把已有功能收成一个统一、稳定、可维护的产品外壳。**

最优先要做好的五件事是：

1. 文案编码
2. 设计 token
3. 数据源统一
4. 页面骨架统一
5. 共享 hook / 共享组件收口

这五件事做好后，后续无论是前端设计重构、功能补完，还是继续接入 Supabase，都会顺很多。
